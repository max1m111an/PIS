// ЛАБОРАТОРНАЯ РАБОТА №1
// ЗАДАНИЕ №5: Область видимости (scope)
// Вариант 04
//
// Выполняйте задания строго по порядку. Запуск файла: node var04.js
// (или кнопкой запуска расширения Code Runner в VS Code)

// --- Лёгкий уровень ---

// TODO 1: Глобальная область видимости
// Объявите глобальную переменную appVersion (любое значение).
// Напишите функцию printVersion(), которая внутри себя выводит
// appVersion через console.log. Вызовите эту функцию.

let appVersion = "app"

function printVersion(){
  console.log(appVersion)
}
printVersion()

// TODO 2: Область видимости функции
// Напишите функцию calculateArea(width, height), которая
// объявляет внутри себя переменную area = width * height и
// ВОЗВРАЩАЕТ её (через return). Вызовите calculateArea(14, 9)
// и выведите результат в консоль.
// Затем попробуйте вывести area ВНЕ функции — допишите эту попытку,
// но сразу закомментируйте её и объясните в выводах результата в отчёте, что
// произойдёт при запуске и почему.

function calculateArea(width, height){
  let area = width * height
  return area
}
console.log(calculateArea(14, 9))
//console.log(area)

// --- Средний уровень ---

// TODO 3: Блочная область видимости (let)
// Раскомментируйте блок кода ниже

if (true) {
  let blockVar = "значение внутри блока";
  console.log(blockVar);
}
//console.log(blockVar)

// Допишите (и сразу закомментируйте) строку console.log(blockVar)
// СНАРУЖИ блока if. В выводах результата в отчёте объясните, что произойдёт при
// попытке раскомментировать эту строку и почему.


// TODO 4: Утечка var из блока
// Раскомментируйте код ниже и запустите

if (true) {
  var leaked = "значение протекло наружу";
}
console.log(leaked);

// В выводах результата в отчёте объясните, почему это сработало (в отличие от TODO 3
// с let), и почему такое поведение var считается плохой практикой.


// --- Сложный уровень ---

// TODO 5: Область видимости внутри вызовов функции
// Напишите функцию makeCounter(), которая объявляет внутри себя
// let count = 0 и возвращает count (просто return count). Вызовите
// makeCounter() дважды подряд, выведите оба результата в консоль.
// В выводах результата в отчёте объясните, почему результаты одинаковые.

function makeCounter(){
  let count = 0
  return count
}

console.log(makeCounter())
console.log(makeCounter())

// --- Задача уровня собеседования ---

// TODO 6: Тень переменной (shadowing)
// Что выведет следующий код? Запишите прогноз в выводах результата в отчёте
// ДО запуска, затем раскомментируйте и проверьте.

let x = 10;
function change() {
  let x = 20;
  console.log(x);
}
change();
console.log(x);
